var a,b,c:integer;
    ch:char;
begin
     read(a);
     write(a);
     write("The number is ",a);
     
     read(ch);
     write(ch);
     write("The char is ",ch);

     read(a,b,c);
     write("The area of this picture",(a + b - c) * c / 2);
end.